# Streamlit Cloud — inference-only dependencies.
# Do NOT add boruta / imbalanced-learn here — they're training-time only
# and would unnecessarily bloat the deployment environment.

streamlit>=1.32.0,<2.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<1.6.0
joblib>=1.3.0,<2.0.0
matplotlib>=3.7.0,<4.0.0
shap>=0.44.0,<0.46.0
