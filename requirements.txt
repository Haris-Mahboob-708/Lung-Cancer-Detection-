# ── Unpickling-critical: must be present and version-compatible to load model_bundle.joblib ──
# The bundle contains a CatBoostClassifier (stage2_model) and a scikit-learn
# LogisticRegression (platt_scaler). Both packages must be importable at joblib.load
# time, or Streamlit raises ModuleNotFoundError before the app can start.
catboost==1.2.10
scikit-learn==1.5.2          # bundle was trained on 1.5.2; pinning avoids cross-version unpickle issues
numpy<2.0.0                  # 1.x for compatibility with the pinned sklearn/catboost build

# ── App runtime dependencies (imported by app.py) ──
streamlit>=1.32
joblib>=1.3
pandas>=2.0
matplotlib>=3.7
plotly>=5.18
shap==0.45.1                 # 0.45.x works with numpy<2; shap 0.52+ requires numpy>=2 and would break the numpy pin above
